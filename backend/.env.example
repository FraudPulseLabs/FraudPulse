# Copy to `.env` and fill in values from Supabase: Project Settings → Database.
# SQLAlchemy + psycopg v3 URI format:
DATABASE_URL=postgresql+psycopg://postgres.YOUR_PROJECT_REF:YOUR_DB_PASSWORD@YOUR_POOLER_HOST.supabase.com:5432/postgres?sslmode=require
